// Next 16 прибрав команду `next lint`, а разом з нею — автогенерацію конфігу.
// Тому flat config тепер живе тут явно, а `npm run lint` кличе eslint напряму.
//
// ⚠️ Лінт у цьому репозиторії ДО 06.09.2026 не працював узагалі (`next lint` падав
// із «next: not found» ще до того, як дійти до правил). Тому перший же запуск дав
// 22 помилки в коді, який давно в проді. Дві з них свідомо переведені в попередження
// — з причинами нижче, — щоб CI ловив НОВІ проблеми вже зараз, а не чекав на
// рефакторинг політики й згоди. Це не «вимкнули, щоб позеленіло»: обидва правила
// далі друкуються в кожному прогоні.
import next from 'eslint-config-next/core-web-vitals'
// Окремий експорт: сам core-web-vitals НЕ вмикає набір @typescript-eslint, тобто
// без нього немає навіть no-unused-vars — мертвий імпорт чи забутий після
// рефакторингу хук не впав би ні в лінті, ні в tsc (noUnusedLocals у нас теж немає).
import ts from 'eslint-config-next/typescript'

const config = [
  {
    // docs/ — заморожені експорти чужих проєктів (studio-export), не наш код.
    ignores: [
      '.next/**',
      'out/**',
      'build/**',
      'node_modules/**',
      'next-env.d.ts',
      'docs/**',
      'public/**',
      // Кожен .claude/worktrees/* — ПОВНА копія цього ж репозиторію (git worktree
      // агента). Без цього рядка eslint лінтить проєкт по разу на кожен воркtree:
      // заміряно 2026-09-07 — 268 файлів і 2067 помилок проти 14 реальних у src/.
      // CI цього не бачить (свіжий checkout не має воркtree), тому проблема
      // мовчазна: локальний лінт зелений у CI й нечитабельний у розробника.
      '.claude/worktrees/**',
      // Артефакти збірки, у git не входять.
      'dist/**',
    ],
  },
  ...next,
  ...ts,
  {
    rules: {
      // Правило вимагає екранувати ' і " в JSX-тексті. У нас там українська
      // юридична копія політики конфіденційності: 20 спрацювань на апострофах
      // у словах на кшталт «зобов'язання». Заміна кожного на &apos; нічого не
      // змінює в рендері, зате робить текст політики нечитабельним у джерелі
      // й підвищує шанс зіпсувати саме той документ, який має бути точним.
      'react/no-unescaped-entities': 'off',

      // ConsentOptOut і CookieConsent читають cookie/localStorage в useEffect і
      // одразу роблять setState. Для react-hooks v7 це антипатерн, але тут він
      // навмисний: значення існує лише в браузері, а обчислення його під час
      // рендеру дало б розбіжність гідратації. Коректна заміна —
      // useSyncExternalStore із серверним снапшотом, тобто переписування логіки
      // згоди. Робити це разом із правками конверсії не можна: згода — юридична
      // поведінка, її треба міняти окремо й перевіряти окремо.
      'react-hooks/set-state-in-effect': 'warn',

      // Заради цього правила набір @typescript-eslint і підключений: воно ловить
      // мертвий імпорт і забуту змінну після рефакторингу. Зараз порушень нуль,
      // тому одразу 'error' — щоб не накопичити нових.
      '@typescript-eslint/no-unused-vars': ['error', { argsIgnorePattern: '^_' }],

      // 13 спрацювань, усі — `(await getMessages()) as Record<string, any>`.
      // Це усталений спосіб читати словник next-intl на сервері в цьому проєкті:
      // типів для повідомлень ми не генеруємо, а вручну описати всю структуру
      // uk.json означало б третю копію тих самих даних. Лишаємо видимим
      // попередженням: захочемо прибрати — це окрема задача з генерацією типів.
      '@typescript-eslint/no-explicit-any': 'warn',
    },
  },
]

export default config
