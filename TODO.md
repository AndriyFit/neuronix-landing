# Neuronix Landing — TODO
> last_updated: 2026-08-25

## Правила оновлення
- Атомарні задачі (одна дія). Часто декомпозиція поточної фази PLAN.md.
- Статус міняється В ТІЙ САМІЙ задачі; зроблено → ✅ + дата.
- «Зроблено» періодично зливаємо в notes.md.
- Статуси: 🔲 · 🟡 · ✅ · 🔴.

## Зараз

- 🔲 **Ads: підняти бюджет до 300 грн — ПІСЛЯ зростання QS, не раніше.**
  Пробували 2026-08-25, відкотили того ж дня. Причина: з 200 грн витрачається 173,
  втрати показів через бюджет 5,7% проти 52,5% через Ad Rank. Доки ранг обмежує,
  додаткові гроші йдуть у ті самі аукціони. Тригер: `post_click_quality_score`
  зрушив із BELOW_AVERAGE або `search_budget_lost_impression_share` перевищив ~15%.

- 🟡 **Ads: перевірити гіпотезу keyword-level URL (2026-08-25 + 2–3 тижні).**
  Щотижня `./scripts/ads-keyword-urls.py snapshot` + звіт `landing_page_view`.
  Якщо покази йдуть на нові URL, а `post_click_quality_score` стоїть — гіпотеза хибна,
  переходити до перестворення RSA по одній групі за раз.
  Гейт календарний, не «за результатами»: ~4 кліки/день сигналу не дадуть.

- 🟡 **Ads: RSA до 15 заголовків + display path — після гейта вище.**
  Зараз усі оголошення мають 10 заголовків із 15 і порожні `path1`/`path2`.
  Свідомо відкладено: перестворення в кампанії на 12-й день додає модерацію
  й перехідний період у вибірку з 39 кліків.

- 🔲 **Перевірити, чи `phone_click` доїжджає в GA4.**
  2026-08-25 додано `sendGTMEvent({event:'phone_click'})` у `Footer.tsx`. Подія гарантовано
  йде в dataLayer, але чи є в GTM тег GA4 Event на цю назву — НЕ перевірено.
  Як перевірити: клікнути телефон у футері на проді → GA4 Realtime → шукати `phone_click`.
  Немає — завести тег у GTM. Пріоритет низький: за 8 місяців телефон не клікнули жодного разу
  (0 подій при 198 page_view), тобто канал і так мертвий.

- 🟡 **Лід Максим (чай) — КП надіслано на погодження Андрію.**
  Документ: `docs/leads/2026-08-20-maksym-tea-proposal.md` + Google Doc
  `11dWdVMnS3QuAG7h087gU7i-j2b4P2ARalEz7pk6L4FA`.
  Два варіанти: Хорошоп B2B (рекомендуємо) і OpenCart, окремим блоком — каталог/контент/майстер-фід.
  Далі: погодити цифри з Андрієм → надіслати клієнту → уточнити фото (є свої чи знімаємо).

- ❌ **ЗАКРИТО 20.08 як НЕПОТРІБНЕ: імпорт GA4 key event `generate_lead` у Ads.**
  Задача трималась на висновку 19.08, що дія `7719736996` мертва. Висновок спростовано:
  · дія **зафіксувала конверсію 19.08** — тобто працює;
  · `tag_snippets` у неї **ідентичні** до `telegram_click`, який точно є GA4-імпортом
    і точно працює, тож WEBPAGE-снипет нічого не доводить (Google віддає його всім
    веб-діям); `type: UNKNOWN` — нерозпізнаний enum, а не `WEBPAGE`.
  Запис «0 конверсій за весь час, втрачати нічого» був **фактично хибний**.
  Виконання плану дало б: другу дію на ту саму подію = **подвійний облік**,
  а видалення наявної знищило б конверсію 19.08. Деталі й урок — `CLAUDE.md`
  → «СПРОСТОВАНО (2026-08-20)».

- 🔴 **Справжня проблема, виявлена при цьому розборі: реклама не приводить заявок.**
  За 14 днів `generate_lead` у GA4 — 3 події, з них з **`cpc` рівно 0**
  (`organic` 1, `referral` 1, `(not set)` 1). Єдина конверсія, яку Ads зарахував
  19.08, прийшла за GCLID у 90-денному вікні, а не з сесії з реклами.
  Тобто трекінг справний, а от кампанія лідів не генерує — розбиратись треба тут,
  а не в конверсіях. Плюс `referral` — це переважно `vercel.com` (70 переглядів
  за 14 днів), тобто ймовірно власні заходи з дашборда Vercel, які засмічують
  статистику; варто відфільтрувати внутрішній трафік у GA4.
- ✅ 2026-08-18 **`telegram_click` тепер у метриці «Конверсії»** — ціль `CONTACT`
  переведено в `biddable: true` (dry-run → apply → звірено перечитуванням).
  Ставок не чіпає: `TARGET_SPEND` конверсіями не керує. Історичні 15-16.08
  Google перерахує із затримкою.

- ✅ 2026-08-18 **Фіксацію лідів задеплоєно** (`256f824`). Наскрізь перевірено на проді:
  форма → `id=3`, вебхук бота → `id=4`, обидва тестові записи прибрано.
  У базі лишається справжній лід Тетяни (`id=2`).

- 🔲 **Точна конверсія на ФАКТ написання** (наступний етап, рішення Андрія 18.08).
  Проблема: `telegram_click` рахує клік, а не клієнта — 2 кліки дали 1 лід, а другий
  візит Тетяни не дав конверсії взагалі. План:
  1. на сайті зберігати `gclid`, кнопка Telegram веде на `t.me/neuronixjhbot?start=<id>`
     (payload обмежений 64 символами, тому в нього йде короткий id, а `gclid` лягає в D1);
  2. `/start` з payload → зв'язати `gclid` з `tg_chat_id` у D1;
  3. коли людина реально пише → offline click conversion у Ads з цим `gclid`.
  Побічний виграш: стане видно, з якого ключового слова прийшов конкретний клієнт.


- ✅ 2026-08-18 **Ліди фіксуються в Cloudflare D1** (`neuronix-leads`,
  `75d3b6c1-0559-4d80-a0cf-38ad0e25065a`, EEUR). Раніше лід жив в одному екземплярі —
  повідомленням у Telegram. Пишуть обидва шляхи: форма (`api/contact`) і бот
  (`api/telegram`, включно з `/start`). Модуль `src/lib/leads.ts`, доступ через
  D1 REST API (Vercel не має binding). Env у Vercel Production виставлені.
  Лід Тетяни (@tetyanazelencova) внесено вручну — `id=2`.

- 🔲 **Андрію:** звузити `CF_D1_TOKEN`. Зараз там широкий
  `abertime/cloudflare_api_token`, яким деплояться ВСІ воркери. Потрібен окремий
  токен лише з `D1:Edit` на `neuronix-leads` → замінити у Vercel + Vault.
  Через API створити не вийшло — Cloudflare вимагає user-level авторизації.

- ✅ 2026-08-18 **Розібрано «конверсія була, а в Ads не видно»** — трекінг справний,
  розриву між GA4 і Ads немає. Деталі в розділі нижче.


- ✅ 2026-08-15 **Пошта на домені `hello@neuronics.work` піднята** (Cloudflare Email
  Routing, $0). Раніше MX у зоні не було взагалі — доменної адреси не існувало.
  · MX `route1/2/3.mx.cloudflare.net`, SPF `include:_spf.mx.cloudflare.net`, DKIM `cf2024-1`.
  · Правило `hello@` → `fitlife2404@gmail.com` + **catch-all** (листи на `info@`,
    `andriy@` тощо не губляться). Destination був верифікований ще 18.06.
  · Перевірено на SMTP: `RCPT TO:<hello@neuronics.work>` → `250`, довільна адреса
    на домені → `250`, чужий домен → `550`. Тест «лист сам собі» непридатний —
    Gmail дедуплікує за `Message-ID`.

- ✅ 2026-08-15 **Адресу замінено на сайті**: `fitlife2404@gmail.com` →
  `hello@neuronics.work`, 22 входження у 7 файлах — політика (16), футер,
  schema.org, i18n uk/en, Cornix-презентація (`public/cornix/index.html` +
  `docs/2026-08-14-cornix-partners-offer.html`). `tsc --noEmit` чистий, JSON валідний.
  **Не закомічено і не задеплоєно** — чекає рішення Андрія (деплой = push у `main`).

- 🔲 **Андрію (2 хв):** Gmail → Settings → Accounts → «Send mail as» → додати
  `hello@neuronics.work`. Код підтвердження прийде на `hello@` (тобто у твій же
  інбокс через пересилання). **Без цього відповідь клієнту піде з особистого Gmail
  і вся заміна адреси втрачає сенс на першій же відповіді.**

- 🔲 **Андрію:** зареєструвати Resend (у Vault ключа немає) → додати домен
  `neuronics.work` → DKIM/SPF-записи я пропишу в CF → ключ у Vault + `RESEND_API_KEY`,
  `LEAD_EMAIL_TO`, `LEAD_EMAIL_FROM` у Vercel. Дає дві речі одразу:
  (1) **дубль заявок на пошту** — код у `src/app/api/contact/route.ts` уже написаний
      і чекає лише env, зараз лід існує в одному екземплярі (тільки Telegram);
  (2) відправку з `hello@` без позначки «via gmail.com».

- 🔲 Андрію: підтвердити доставку — надіслати лист на `hello@neuronics.work`
  з БУДЬ-ЯКОЇ іншої адреси (не з `fitlife2404@gmail.com`, інакше Gmail дедуплікує).

- ✅ 2026-08-13 Кампанія **`Search — Сайти та магазини (UA)`** id `24138448702` створена
  **PAUSED** (AdLoop, за `docs/2026-08-13-ads-campaign-plan.md`). Бюджет 200 грн/день,
  Maximize Clicks зі стелею CPC 60 грн, гео UA `2804`, мови uk+ru, без Search Partners
  і Display expansion. Групи: `Інтернет-магазин` `202276594747`, `Сайт під ключ`
  `199656753776`, `Ціна розробки` `202948761487` — 10 ключів разом. 38 мінус-слів
  PHRASE на рівні кампанії. 3 RSA (по одному на групу), final_url `https://neuronics.work/uk`,
  статус модерації `REVIEW_IN_PROGRESS`. Усе звірено перечитуванням через GAQL.
- ✅ 2026-08-13 Білінг акаунта — `APPROVED` (payments profile «Андрій Гордієнко»).
- ✅ 2026-08-14 Кампанія **ЗАПУЩЕНА**. Усі 3 RSA `ENABLED` + `APPROVED` (звірено GAQL).
  Модерація була пройдена ще 13.08 — «Не відповідає вимогам» у UI означало
  `AD_GROUP_ADS_PAUSED`, а не відхилення.
- ✅ 2026-08-14 Policy-фікси на сайті (гілка `fix/ads-policy-compliance`): чужі реквізити
  ФОП Удич + ІПН прибрано з політики і schema; `mailto:` у футер і в політику; §2 політики
  приведено у відповідність до фактичного Consent Mode; «знеособлені дані» → реальний
  перелік ідентифікаторів; обсяг гарантії 12 міс; строки зведено до FAQ.
  Побічно полагоджено `MISSING_MESSAGE` — кнопка «Дізнатись більше» на `/ai`, `/opencart`,
  `/horoshop`, `/keycrm` рендерила сирий ключ (`Services.tsx` бере `more` з `services`).
- ✅ 2026-08-14 Заміна RSA групи «Інтернет-магазин» завершена: нове `820908332815`
  («Запуск від 4 тижнів») `ENABLED` + `APPROVED`, старе `820780707003`
  («Запуск за 10 днів») переведено в `PAUSED` (AdLoop plan `2d067958`, звірено GAQL).
  Розбіжність строку між оголошенням і FAQ закрита.
- ✅ 2026-08-14 Перевірка «чи не забанила модерація»: НІ. Усі RSA `APPROVED`/`REVIEWED`,
  `policy_topic_entries` порожні, ключі `APPROVED`/`ELIGIBLE`, білінг `APPROVED`,
  акаунт `ENABLED`. «Не відповідає вимогам» у UI — це `campaign.primary_status`
  `NOT_ELIGIBLE` / `AD_GROUP_ADS_PAUSED`: застарілий агрегат рівня кампанії, тоді як
  `ad_group_ad.primary_status` = `ELIGIBLE` у всіх. `change_event` теж відстає —
  не бачить жодної зміни за 14.08. Не лікується, минає само.
- ✅ 2026-08-15 Нуль показів за перші 6 год 14.08 — розсмоктався сам, як і планували
  («дочекатись повної доби»). Search IS 19.4% → **72.7%**, втрата IS через бюджет
  42.9% → **0%**. Правило «якщо покази 0-2 → стеля 120 грн» **не настало, не застосовуємо**.

- ✅ 2026-08-15 **Перші результати кампанії за 2 доби** (14.08 неповна + 15.08 в процесі):

  | | 14.08 | 15.08 | Разом |
  |---|---|---|---|
  | Покази | 27 | 29 | **56** |
  | Кліки | 1 | 4 | **5** |
  | CTR | 3.7% | 13.8% | **8.9%** |
  | Витрата | 32.32 ₴ | 221.66 ₴ | **253.98 ₴** |
  | Сер. CPC | 32.32 ₴ | 55.42 ₴ | **50.80 ₴** |
  | Search IS | 19.4% | 72.7% | — |
  | Втрата IS (бюджет / ранг) | 42.9% / 37.8% | 0% / 27.3% | — |

  · **Перша конверсія: `telegram_click` 15.08** (1 з 5 кліків = 20%). У метриці «Конверсії»
    показує 0 — це за задумом, дія свідомо non-biddable. Трекінг підтверджено наскрізь:
    клік → GTM → GA4 → Ads.
  · CTR 8.9% — удвічі вище норми UA-ніші (3-5%). Оголошення і семантика влучають.
  · Search terms: 12 запитів, **жодного сміття** — 38 мінус-слів працюють.
  · Top IS 57-79% — оголошення у верхньому блоці.
  · Витрата 221.66 ₴ > бюджету 200 ₴ — Google перевитрачає до 2× на день і вирівнює
    по місяцю. Не баг.
  · Акаунт **постоплатний**, поріг оплати ~583 ₴ (`account_budget` APPROVED, нараховано
    200.08 ₴). Кампанія від «балансу 200 грн» НЕ зупиниться — попередній запис про
    «200 грн на добу показів» некоректний.
  · QS: `вартість створення інтернет магазину` 6 · `розробка сайтів під ключ` 6 ·
    `розробка сайту під ключ` 5 · **`створення інтернет магазину` 2** · решта без даних.
  · GA4-поведінку НЕ дивився: у конфізі AdLoop (`~/.adloop/config.yaml`) стоїть property
    Trembita `531732654`, не Neuronix.

- 🔴 **Головний висновок: вузьке місце — не ставки, а обсяг ринку.** При IS 72.7% і
  29 показах доступний пул ≈ **40 показів/добу** на всю семантику → стеля 4-5 кліків/день.
  Підняття `cpc_bid_ceiling` до 120 ₴ дасть максимум +27% показів (~10/добу) за вдвічі
  дорожчий клік — погана угода. **Ставки не чіпати.**

- 🔲 **Розширити семантику — єдиний спосіб дістати обсяг.** Найдешевший хід: нові групи
  під наявні лендінги `/opencart`, `/horoshop`, `/keycrm` — гарячий intent, вужча
  конкуренція, дешевший клік, ніж у «створення сайту». Рішення Андрія 15.08: записати
  в план, не виконувати одразу.

- 🔲 Через 2 повні доби (≈17.08) переглянути `створення інтернет магазину` (QS 2,
  4 покази, 0 кліків) — пауза, якщо QS не підніметься.

- 🔲 Про конверсійність сайту судити ЩЕ НЕ МОЖНА: 5 кліків = статистичний нуль.
  Потрібно ≥100 кліків.
- ✅ 2026-08-14 Телефон `+380685026199` у футері (`tel:`) + `telephone` у schema
  (Organization.contactPoint і ProfessionalService).
- ✅ 2026-08-14 Конверсія «дзвінок» заведена наскрізь: GTM тригер `Phone click`
  (`{{Click URL}}` містить `tel:`) → тег `GA4 Event - phone_click` → опубліковано версією 3;
  GA4 key event `phone_click` (`ONCE_PER_SESSION`, id 15436831105); Ads conversion action
  id `7720846699` `ENABLED`, категорія `PHONE_CALL_LEAD`. Перевірено: живий
  `gtm.js?id=GTM-N4MBTL2W` містить `phone_click` і умову `tel:`.
  ⚠️ `PHONE_CALL_LEAD` — biddable (входить у метрику «Конверсії»), на відміну від
  `telegram_click`. Обґрунтування — `CLAUDE.md` → «Конверсія "дзвінок"».
- 🔲 **Андрію:** клікнути по телефону в футері з мобільного і глянути GA4 Realtime —
  єдина перевірка, яку не можу зробити сам (клік по `tel:` на десктопі відкриває
  системний діалог і вішає автоматизацію браузера).
- 🔲 Опційно: call extension (asset типу CALL) у кампанії `24138448702` — тепер номер є.
- ⚠️ ЗАСТАРІЛО (уточнено 2026-08-15) ~~2026-08-14 Баланс 200 грн — на добу показів~~.
  Акаунт **постоплатний** з порогом оплати ~583 ₴, а не передоплатний: за 2 доби
  витрачено 253.98 ₴ і покази не спинились. «Баланс» показів не обмежує.
- ✅ 2026-08-12 Заявки перевів на брендований `@neuronixjhbot` — Vercel Production env
  оновлено, доставка перевірена. Активується найближчим деплоєм з `main`.
- ✅ 2026-08-12 Профіль бота: опис, короткий опис, `/start`, кнопка «Сайт» (web_app) —
  uk+en, виставлено через Bot API.
- 🔲 **Андрію:** аватар бота — тільки через @BotFather (`/setuserpic` для `@neuronixjhbot`),
  з API не ставиться.
- 🔲 **Андрію:** увімкнути дубль заявки поштою — зареєструвати ключ на resend.com,
  покласти у Vault `neuronix/resend_api_key`, додати у Vercel Production `RESEND_API_KEY`
  і `LEAD_EMAIL_TO` (своя пошта). Без верифікації домену From лишається
  `onboarding@resend.dev` і шле лише на пошту власника акаунта — цього достатньо.
  Код уже готовий: порожні змінні = канал пропускається.
- ✅ 2026-08-13 Google Ads: акаунт **Neuronix `908-703-7980`** створено (UAH, Europe/Kiev —
  обидва незмінні, звірено), прив'язано до MCC Fit-Life `1770392909`, auto-tagging увімкнено.
  GA4 `neuronics.work` (`properties/548742185`) зв'язано з акаунтом.
  Conversion actions (перевірено через API):
  · ⚠️ ВИПРАВЛЕНО 19.08: `generate_lead` id 7719736996 — SUBMIT_LEAD_FORM, ENABLED, primary,
    АЛЕ `type: WEBPAGE` (gtag-тег Ads), а тега `AW-18387717496` на сайті немає — дія
    фізично не могла спрацювати ні разу. Перевіряти `type` і `tag_snippets`, не лише
    статус/категорію. Деталі: `CLAUDE.md` → «Конверсія "заявка з форми" НЕ ДОХОДИЛА в Ads»
  · `telegram_click` id 7719455642 — CONTACT, ENABLED, поза метрикою «Конверсії»
    ⚠️ Виправлення 13.08: попередній запис «secondary» був хибний — API показував
    `customer_conversion_goal CONTACT biddable=True`, тобто клік у Telegram рахувався
    в «Конверсіях» нарівні із заявкою. Виставлено `biddable=False`, перевірено
    перечитуванням. Дія далі відстежується, але не змішується з лідами й не отруїть
    майбутній Smart Bidding.
  · обидві ONE_PER_CLICK (повторний клік не множить конверсію)
  · заготовки Google `purchase`/`qualify_lead`/`close_convert_lead` лишені HIDDEN — порожні
  Червоне «Неправильне налаштування» на цілі «Контакт» — очікуване й після правки 13.08:
  ціль свідомо не бере участі в метриці «Конверсії». Не баг, не чіпати.
  ⚠️ Ключовими події стали 13.08 — рахуються лише з цієї дати, заднім числом GA4 не перерахує.
- ✅ Продуктивність /uk. ЗАКРИТО 13.08.2026, підтверджено 5 повторами PSI (ключ API):
  mobile **99** стабільно (LCP 1,4с · FCP 1,0с · TBT 30мс · SI 3,5с), desktop **99**.
  Корінь минулого разу (GTM 275 КіБ у критичному шляху) — підтвердився: `DeferredGTM`
  вантажить `<GoogleTagManager>` на першу взаємодію (pointerdown/keydown/touchstart/
  scroll) або через 4с fallback. Безпечно для конверсій — `sendGTMEvent` сам створює
  `window.dataLayer` і пушить незалежно від того, чи gtm.js вже стартував; GTM
  обробляє чергу з початку, коли зрештою довантажується. Перевірено локально:
  подія `generate_lead`, відправлена одразу після миттєвого кліку, лишається
  в dataLayer і після ініціалізації. Ризик — тільки вкладка, закрита до того,
  як gtm.js встиг довантажитись.
  Попередній запис «✅ ЗАКРИТО, 98» від учора був хибний (один вдалий прогін) —
  цього разу закриваю на 5/5 стабільних повторах, не на одному.

- 🔲 Продуктивність `/uk/ai`. НЕ ЗАКРИТО. LCP-елемент — той самий H1
  (`hero-title hero-anim hero-anim-1`, спільний Hero.css), фікс opacity з /uk
  тут уже діє. Дві гіпотези перевірено й СПРОСТОВАНО:
  — вага JS не винна: 724 КБ на /uk проти 718 КБ на /uk/ai (білд), майже нуль різниці;
  — кеш не винен: x-vercel-cache HIT на всіх запитах, edge теплий.
  Реальна проблема — розкид результатів сам із собою: 3 виміри PSI поспіль без
  жодних змін коду дали LCP 4,6с / 4,8с / 1,65с. /uk після фіксу GTM стабільний
  99/100 п'ять разів поспіль, /uk/ai так не тримається — корінь розкиду не
  знайдено. Подальші прогони зараз заблоковані: повторні швидкі запити з VPS
  зачепили бот-захист Vercel (rate limit на 403 Security Checkpoint).
- ✅ Додати біля обох форм (`Contact.tsx`, `AuditForm.tsx`) рядок зі згодою на обробку
  персональних даних + лінк на політику — зараз лінк лише у футері.
- 🔲 **Андрію (в GSC, не код):** перевірити що sitemap.xml поданий у Search Console;
  зробити Request Indexing для /uk, /uk/ai, /uk/opencart, /uk/horoshop, /uk/keycrm

## Беклог
- 🔲 Квіз-калькулятор «скільки втрачаєте без автоматизації» на /ai (відкладено 2026-08-09)
- 🔲 Підтвердити в Андрія ціну голосових AI-агентів (на /ai зараз «розрахунок після аудиту»)
- ✅ Кнопка «Дізнатись більше» в Services.tsx захардкоджена укр. мовою — на /en показує укр.

## Зроблено (чистити → notes.md)
- ✅ 2026-08-09: Англійський блог: 14 статей перекладено (`src/content/blog/en/`), блог
  локалізовано (locale-aware API, UI-рядки в i18n, hreflang, sitemap 25→40 URL),
  редіректи /en/blog прибрано
- ✅ 2026-08-09: GSC-аудит: корінь 307→308 (постійний), x-default hreflang скрізь
  (HTML + sitemap). Деталі: CLAUDE.md → «Індексація і hreflang»
- ✅ 2026-08-09: Платформені сторінки /opencart /horoshop /keycrm (uk+en) за інтент-дослідженням:
  спільний шаблон [platform], оффер «контент-система 24/7», футер-перелінковка, sitemap.
  Дослідження: docs/keyword-research-2026-08.md, ітерація 2
- ✅ 2026-08-09: Дослідження пошукових запитів UA (Keyword Planner) + сторінка /ai під AI-оффери:
  i18n `ai.*`, реюз Pains/Services/FAQ через namespace, AiHero/AiStats/AiSecurity, nav, sitemap,
  картка AI на головній. Доки: docs/keyword-research-2026-08.md
- ✅ 2026-08-06: Фікс ривків скролу Hero (`AnimatedTerminal` `min-height` → `height`)
- ✅ 2026-08-05: Clarity consent v2, GTM, дублі блогу, og:image, sitemap
- ✅ 2026-08-04: Конверсійний редизайн головної під дані попиту
